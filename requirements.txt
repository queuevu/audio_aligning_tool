# GUI framework
PySide6>=6.5.0

# Forced-alignment engine
# Note: aeneas also needs system-level dependencies (espeak/espeak-ng and
# ffmpeg) which are not installable via pip. On macOS install them first:
#   brew install espeak ffmpeg
aeneas>=1.7.3.0
numpy>=1.21
